# Committed on purpose. A production build with no VITE_API_URL in the
# environment — a Vercel deploy from a fresh clone, say — would otherwise bake
# in localhost:8000 and call nothing. A real env var still wins over this file,
# so the Docker build arg and any dashboard setting override it.
VITE_API_URL=https://hrn.ultronai.me
