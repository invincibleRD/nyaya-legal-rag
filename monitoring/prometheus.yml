# scrape the api, which exposes everything at /api/v1/metrics in prometheus format
#   docker compose -f docker-compose.yml -f monitoring/docker-compose.monitoring.yml up -d
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: nyaya-api
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ["api:8000"]

  - job_name: qdrant
    metrics_path: /metrics
    static_configs:
      - targets: ["qdrant:6333"]
